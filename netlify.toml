[build]
    publish = "/build"
    command = "./create_docs.sh && mkdocs build -d build/docs"

[[redirects]]
    # This one redirects all non-existent pages to the redux dev docs,
    # with an "are you sure?" page in between.
    from = "/*"
    to = "/redirect-to-redux-site.html#!:splat"
    status = 302